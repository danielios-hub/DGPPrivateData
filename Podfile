# Uncomment the next line to define a global platform for your project
platform :ios, '13.0'
use_frameworks!

def app_pods
  pod 'PromisesSwift'
  pod 'Kingfisher', '~> 5.0'	
end 

target 'DGPPrivateData' do
  app_pods
end
